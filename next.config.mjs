/** @type {import('next').NextConfig} */
const nextConfig = {
	typescript: {
		// ⚠️ WARNING: Disables all type-checking errors at build time
		ignoreBuildErrors: true,
	},
};

export default nextConfig;
